#!/bin/bash
# Add org file changes to the repository

export PATH="/home/matus/.local/bin:$PATH"

echo "Repository: org"
cd ~/org
# Remove deleted files
git ls-files --deleted -z | xargs -0 git rm
# Add new files
git add .
git commit -m "thales: $(date --iso-8601=seconds)"
git push origin thales
